version: "3.9"

services:
  anvil:
    build: .
    container_name: anvil_api
    ports:
      - "8000:8000"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - ANVIL_OUTPUT_DIR=/app/outputs
    volumes:
      - ./outputs:/app/outputs
    restart: unless-stopped
